<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <header>
      <% include partials/_header%>
    </header>
    <ul>
    <% for(let i in urls) { %>
      <li><%= `${i} : ${urls[i]}` %></li>
    <% } %>
    </ul>
</body>
</html>